name = "    Mr Ayo    "
# print(len(name))
# print(name.upper())
# stripedName = name.strip()
# print(stripedName)

    #Reaplace
sentence = "There is only one optional parameter in it. chars – a string specifying the set of characters to be removed. If the optional chars parameter is not given, all leading and trailing whitespaces are removed from the string."
word = input("Emter word to search: ")
if word in sentence:
    print(sentence.count(word))
print(sentence.replace("in",word))

dim = (2,10,30)
# if "fidelis" in sentence:
#     print("It exist")

# for char in name:
#     print(char)